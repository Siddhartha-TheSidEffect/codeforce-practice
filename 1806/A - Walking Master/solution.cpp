#include <bits/stdc++.h> using namespace std; // --- FAST I/O & SHORTCUTS ---using ll = long long;using vll = vector<long long>;using vi = vector<int>;using pii = pair<int, int>; // Constantsconst ll MOD = 1e9 + 7;const ll INF = 1e18; // --- LOGIC ---void solve() {    int a,b,c,d;    cin >> a >> b >> c >> d;    if(d-b>= 0){        int vertical = d-b;        if(a+vertical-c>=0){            int hori=a+vertical-c;            cout<<hori+vertical<<"\n";            return;        }        else{            cout << -1 << "\n";            return;        }    }    else{        cout << -1 << "\n";        return;    }         } // --- MAIN DRIVER ---int main() {    // Fast I/O: 10x faster input reading    ios_base::sync_with_stdio(false);    cin.tie(NULL);     int T = 1;    cin >> T; // Comment this out if problem has NO test cases    while (T--) {        solve();    }    return 0;}