#include <bits/stdc++.h>using namespace std;using ll = long long;using vll = vector<long long>;using vi = vector<int>;using pii = pair<int, int>;const ll MOD = 1e9 + 7;const ll INF = 1e18;void solve() {    string n;    cin >> n;    if(n[0]==n[n.length()-1]){        cout << n<<endl;    } else {        n[0] = n[n.length()-1];        cout << n<<endl;    }        }int main() {    ios_base::sync_with_stdio(false);    cin.tie(NULL);     int T = 1;    cin >> T;     while (T--) {        solve();    }    return 0;}